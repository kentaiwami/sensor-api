apiVersion: v1
kind: Service
metadata:
  name: sensor-api
  namespace: sensor-api
spec:
  type: NodePort
  selector:
    app: sensor-api
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 32224
